const help = (prefix) => {
  return `
[Main]
> ${prefix}help
> ${prefix}menu
> ${prefix}?

[Fun]
> ${prefix}meme
`
}

const sayhello = () => {
  return `Halo `
}

const gcwelcome = () => {
  return `Selamat datang di group `
}

const saygoodbye = () => {
  return `Sampai jumpa `
}
